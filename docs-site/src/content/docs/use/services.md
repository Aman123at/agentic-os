---
title: Services and ports
description: Programs Agents keep running, and how to open one in your browser.
---

STUB — page map only. This page exists so the sidebar and the shape of the site are
judgeable; M6 writes it.

Must carry: that a forwarded Service is opened **from the Desktop**, not by typing a URL —
`/port/<n>/` carries a single-use ticket, so a hand-typed address refuses — and where in the
Desktop that link is. Compose users keep `<port>.localhost`.
