---
title: "aos config"
description: "Show or change settings (stored in /etc/aos/config.yml)"
---

{/* Generated by tools/docsgen. Do not edit: `go run ./tools/ci lint` fails on drift. */}

Show or change settings (stored in /etc/aos/config.yml)

### `aos config get`

Print one setting's value

```
aos config get <key>
```

### `aos config list`

List every setting, its value and where it comes from

```
aos config list
```

### `aos config set`

Change a setting (an empty value "" resets it to the default)

```
aos config set <key> <value>
```

