---
title: "aos uninstall"
description: "Remove the systemd unit and the binary (--purge also deletes the account and all data)"
---

{/* Generated by tools/docsgen. Do not edit: `go run ./tools/ci lint` fails on drift. */}

Remove the systemd unit and the binary (--purge also deletes the account and all data)

```
aos uninstall [flags]
```

Options:

```
      --purge   also delete the aos account, /home/aos, /var/lib/aos and the config
      --yes     actually remove things; without it, only print what would be removed
```

