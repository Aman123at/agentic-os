---
title: Run it with Docker Compose
description: "The sandboxed alternative: same product, container filesystem."
---

STUB — page map only. This page exists so the sidebar and the shape of the site are
judgeable; M6 writes it.

Must carry: that the container is the filesystem Agents see, not the server's; that
`sudo aos browser install` is how Compose users get the Browser too, because the published image
no longer carries Chromium; that the Shared Folder no longer exists; that `<port>.localhost` still
works here, because the host really is localhost.
