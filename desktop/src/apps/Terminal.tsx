// Terminal — xterm.js over User Sessions. The real app lands in M3.3; this
// placeholder proves the window, Dock entry and lazy chunk wiring.
export default function Terminal() {
  return (
    <div className="placeholder">
      <p className="placeholder__icon">⌨️</p>
      <p>Terminal arrives in M3.3.</p>
    </div>
  );
}
