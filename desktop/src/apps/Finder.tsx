// Finder — file browsing over FileService. The real app lands in M3.2; this
// placeholder proves the window, Dock entry and lazy chunk wiring.
export default function Finder() {
  return (
    <div className="placeholder">
      <p className="placeholder__icon">🗂️</p>
      <p>Finder arrives in M3.2.</p>
    </div>
  );
}
