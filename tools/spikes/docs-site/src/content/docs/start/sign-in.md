---
title: Sign in and set your password
description: The first sign-in, the forced password change, and where the account lives.
---

STUB — page map only. This page exists so the sidebar and the shape of the site are
judgeable; M6 writes it.

Must carry: the twelve-character minimum, refused rather than warned; the forced first change, and
that it blanks the password out of `config.yml`; that `username` in `config.yml` is authoritative,
so renaming the account is a config edit; that changing the password signs every other session
out; the lockout message and its three escapes.
