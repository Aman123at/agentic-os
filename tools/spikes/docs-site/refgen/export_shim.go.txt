// THROWAWAY SPIKE, and it never lands in the working tree: run.sh maps this file
// into package cli with `go run -overlay`, so the prototype can walk the real
// command tree without editing internal/cli before M6 is approved.
//
// M6 does the honest version instead: rename rootCmd to Root at
// internal/cli/cli.go:40 and delete this file.
package cli

import "github.com/spf13/cobra"

// Root is the aos command tree, for the reference generator.
func Root() *cobra.Command { return rootCmd() }
