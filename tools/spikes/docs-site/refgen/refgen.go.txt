// Command refgen writes the documentation site's command reference from the aos
// cobra tree, one page per top-level command plus an index.
//
// THROWAWAY SPIKE. Written for .scratch/native-linux-release/issues/13-documentation-site.md
// to show what a generated reference looks like against the real command tree.
// M6 writes the shipped generator at tools/docsgen.
//
//	sh tools/spikes/docs-site/refgen/run.sh [outdir]
//
// It deliberately does not import github.com/spf13/cobra/doc: that package pulls
// go-md2man and blackfriday into the module graph (go.sum carries both today as
// /go.mod-only hashes, so neither is built), its output carries "### SEE ALSO"
// boilerplate, and it cannot emit Starlight frontmatter.
package main

import (
	"fmt"
	"os"
	"path/filepath"
	"sort"
	"strings"

	"github.com/spf13/cobra"

	"github.com/amantiwari/agentic-os/internal/cli"
)

const banner = "{/* Generated by tools/docsgen. Do not edit: `go run ./tools/ci lint` fails on drift. */}"

func main() {
	out := "src/content/docs/reference"
	if len(os.Args) > 1 {
		out = os.Args[1]
	}
	if err := os.MkdirAll(out, 0o755); err != nil {
		fail(err)
	}
	root := cli.Root()
	var tops []*cobra.Command
	for _, c := range root.Commands() {
		if c.Hidden || c.Name() == "help" || c.Name() == "completion" {
			continue
		}
		tops = append(tops, c)
	}
	sort.Slice(tops, func(i, j int) bool { return tops[i].Name() < tops[j].Name() })

	for _, c := range tops {
		var b strings.Builder
		frontmatter(&b, "aos "+c.Name(), c.Short, 0)
		b.WriteString(banner + "\n\n")
		section(&b, c, 2)
		write(filepath.Join(out, c.Name()+".mdx"), b.String())
	}

	var b strings.Builder
	frontmatter(&b, "Command reference", "Every aos command, generated from the binary", 0)
	b.WriteString(banner + "\n\n")
	b.WriteString("Run `aos` with no command to start an interactive chat: each message is a Task.\n\n")
	b.WriteString("| Command | What it does |\n| --- | --- |\n")
	for _, c := range tops {
		b.WriteString(fmt.Sprintf("| [`aos %s`](./%s/) | %s |\n", c.Name(), c.Name(), c.Short))
	}
	write(filepath.Join(out, "index.mdx"), b.String())
	fmt.Printf("refgen: %d pages in %s\n", len(tops)+1, out)
}

// section writes one command and recurses into its subcommands, so a group like
// `aos service` stays on one page instead of scattering into a directory.
func section(b *strings.Builder, c *cobra.Command, level int) {
	if level > 2 {
		fmt.Fprintf(b, "%s `%s`\n\n", strings.Repeat("#", level), c.CommandPath())
	}
	if long := strings.TrimSpace(c.Long); long != "" {
		fmt.Fprintf(b, "%s\n\n", long)
	} else if short := strings.TrimSpace(c.Short); short != "" {
		fmt.Fprintf(b, "%s\n\n", short)
	}
	// A group with no RunE has nothing to run, so its usage line is noise.
	if c.Runnable() {
		fmt.Fprintf(b, "```\n%s\n```\n\n", strings.TrimSpace(c.UseLine()))
	}
	if flags := strings.TrimRight(c.NonInheritedFlags().FlagUsages(), "\n"); flags != "" {
		fmt.Fprintf(b, "Options:\n\n```\n%s\n```\n\n", flags)
	}
	if ex := strings.TrimSpace(c.Example); ex != "" {
		fmt.Fprintf(b, "Example:\n\n```sh\n%s\n```\n\n", ex)
	}
	subs := c.Commands()
	sort.Slice(subs, func(i, j int) bool { return subs[i].Name() < subs[j].Name() })
	for _, s := range subs {
		if s.Hidden || s.Name() == "help" {
			continue
		}
		section(b, s, level+1)
	}
}

func frontmatter(b *strings.Builder, title, description string, _ int) {
	fmt.Fprintf(b, "---\ntitle: %s\ndescription: %s\n---\n\n", yamlString(title), yamlString(description))
}

// yamlString quotes a scalar the blunt way: the strings here are command help,
// never multi-line, and a generator that guesses wrong produces a site that
// does not build.
func yamlString(s string) string {
	return `"` + strings.NewReplacer(`\`, `\\`, `"`, `\"`).Replace(strings.TrimSpace(s)) + `"`
}

func write(path, body string) {
	if err := os.WriteFile(path, []byte(body), 0o644); err != nil {
		fail(err)
	}
}

func fail(err error) {
	fmt.Fprintln(os.Stderr, "refgen:", err)
	os.Exit(1)
}
